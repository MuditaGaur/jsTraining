
var assert = require('assert');
describe('https://www.google.com', function(err, body, res) {
    it('should return statuscode when url is correct', function() {
      assert.equal(response.statuscode, statuscode);
    });
  });
